import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.petinformationmanager',
  appName: 'Pet Information Manager',
  webDir: 'dist'
};

export default config;
